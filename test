#!/usr/bin/perl -w

use strict; use warnings FATAL => 'uninitialized';

use Test::Harness;

# make sure not to carry over a TEST=0 setting, which would make
# Chj::TEST based testing fail
$ENV{TEST}=1;

our @t=
  qw(
	require_and_run_tests
	testlazy
	testlazy10
	ftemplate-test
	csv2xml
	intro-basics
	examples-fibs
	examples-primes
	skip-internal
	skip
	skip-leak
	csvstreams
	bugs-perl-weaken-coderef
   );

runtests(map {"t/$_"} @t);
