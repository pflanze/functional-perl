#!/usr/bin/env perl

use strict; use warnings; use warnings FATAL => 'uninitialized';

$ENV{RUN_TESTS}=1; exec "functional_XML/t/pxml-text" or exit 1;

# XX run functional_XML/t/stream as well? That one is slow, though.
